library(mgcv)
library(parallel)
## Sourcing model fitting functions.
source("Rfunc.R")
source("full-covs.R")
## Reading in data.
load("manta.RData")

covs$unlag.mei <- c(covs$mei[-1], 0)
covs$en.factor <- c(rep("No", 5), rep("Yes", 2), rep("No", 4))

## A model with fit.popan ####
fit.full <- fit.popan(captlist[1:2], model.list = list(b = ~ occasion,
                                                        phi = ~ occasion,
                                                        p = ~ occasion),
                       group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
                       df = cbind(df, covs), printit = FALSE)

## Trying out par.fit.popan ####
args <- list(
    ## Models with everything varying by occasion.
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion,
    ## except for p (by survery effort)
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion, except for p (by MEI)
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion, except for p (by lag.mei)
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion, 
    ## except for p (by occasion and survey effort)
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion.
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion
    ## plus p varying by lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion
    ## except for p (by survey effort + mei)
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion
    ## except for p (by survey effort + lag.mei)
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion.
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with everything varying by occasion.
    ## plus p (by survey effort + lag.mei)
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    
    ## Models with everything varying by occasion
    ## except for b (by mei)
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, phi by occasion, and 
    ## p by survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b and p varying by mei, phi by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, phi by occasion, and 
    ## p by lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, phi by occasion, and 
    ## p by occasion + survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, phi by occasion, and 
    ## p by occasion + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, phi by occasion, and 
    ## p by occasion + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, phi by occasion, and 
    ## p by survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, phi by occasion, and 
    ## p by survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, phi by occasion, and 
    ## p by occasion + survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, phi by occasion, and 
    ## p by occasion + survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, phi and p by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, phi by occasion
    ## p by survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),

    ## Models with b varying by lag.mei, phi by occasion
    ## p by mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b and p varying by lag.mei, phi by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, phi by occasion
    ## p by occasion + survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, phi by occasion
    ## p by occasion + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, phi by occasion
    ## p by occasion + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, phi by occasion
    ## p by survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, phi by occasion
    ## p by survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, phi by occasion
    ## p by occasion + survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, phi by occasion
    ## p by occasion + survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, phi by occasion
    ## p by survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, phi by occasion
    ## p by mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, phi by occasion
    ## p by lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, phi by occasion
    ## p by occasion + survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b and p varying by occasion + mei, phi by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, phi by occasion
    ## p by occasion + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, phi by occasion
    ## p by survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, phi by occasion
    ## p by survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, phi by occasion
    ## p by occasion + survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, phi by occasion
    ## p by occasion + survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
 
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by occasion + survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by occasion + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by occasion + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by occasion + survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),

    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, phi by occasion
    ## p by occasion + survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ occasion,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),

    ## Models with b varying by occasion, constant phi
    ## p by mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by occasion + survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by occasion + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by occasion + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),

    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by occasion + survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion, constant phi
    ## p by occasion + survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b and p varying by mei, constant phi
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by occasion + survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by occasion + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by occasion + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by occasion + survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by mei, constant phi
    ## p by occasion + survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by occasion + survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by occasion + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by occasion + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by occasion + survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by lag.mei, constant phi
    ## p by occasion + survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by occasion + survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b and p varying by occasion + mei, constant phi
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by occasion + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by occasion + survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + mei, constant phi
    ## p by occasion + survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by occasion
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by occasion + survey effort
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by occasion + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by occasion + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by occasion + survey effort + mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs),
    
    ## Models with b varying by occasion + lag.mei, constant phi
    ## p by occasion + survey effort + lag.mei
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = FALSE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = FALSE, phi = TRUE, p = TRUE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = TRUE),
         df = covs),

    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = TRUE, p = FALSE),
         df = covs),
    list(caplist = captlist[1:2], model.list = list(b = ~ occasion + lag.mei,
                                                    phi = ~ 1,
                                                    p = ~ occasion + survey.eff + lag.mei),
         group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
         df = covs)
)


## best fit ####       
best.fit <- fit.popan(caplist = captlist[1:2], model.list = list(b = ~ lag.mei,
                                                                 phi = ~ 1,
                                                                 p = ~ occasion),
                      group.pars = list(b = TRUE, phi = FALSE, p = FALSE),
                      df = covs)
AIC(best.fit)

## Bootstrapping to calculate CIs ####
## Carrying out a bootstrap procedure. This will take a while, and
## computing time will be proportional to n.boots. While you're
## playing around you can use something like n.boots = 100, but when
## you do this for realsies it's best to use 1000 or even 10000.
boot.best.fit <- boot.popan(best.fit, n.boots = 10000)
## Summary for estimated parameters.
summary.popan(boot.best.fit, function(fit) fit$fit$par)
## Summary for ENs for first group.
summary.popan(boot.best.fit, function(fit) fit$ENs[, 1])
# save boot.best.fit to file
save(boot.best.fit, file = "boot-best-fit-manta.RData")

## Plotting ENs for first group, with CIs ####
ENs.summary <- summary.popan(boot.best.fit, function(fit) fit$ENs[, 1])
## Creating a line for point estimates. The y-axis goes from 0 to the
## highest upper CI limit.
plot(ENs.summary[, 1], type = "l", ylim = c(0, max(ENs.summary[, 4])))
## Adding dotted line for lower and upper CI limits.
lines(ENs.summary[, 3], lty = "dotted")
lines(ENs.summary[, 4], lty = "dotted")

## fits all possible models on the list above ####
fits <- par.fit.popan(3, arg.list = args)
# list AICs of all possible models
sapply(fits, AIC)
# sort AICs of all models from the lowest to the highest
order(sapply(fits, AIC))

do.call("fit.popan", args[[468]])

# list the model and parameter grouping ####
fits[[468]]$args$model.list
# grouping between females and males
# TRUE means FEMALES = MALES, FALSE means FEMALES are different from MALES
fits[[468]]$args$group.pars
## count the number of parameters
length(fits[[468]]$fit$par)
## AIC of the model
AIC(fits[[468]])

# plot all parameters from the best fit ####
plot(fits[[468]])
plot(covs$unlag.mei)

# load boot best fit data
load("boot-best-fit-manta.RData")
